# 我的项目

第 1 周 Day 2 的练习仓库：学会把项目存进 GitHub。

## 当前状态

- `index.html`：占位页，只用来验证「仓库 + 首次提交」这一步能跑通。
- 真正的功能开发从 **Day 7** 开始（那天做 MVP）。

## 目录结构

```
.
├── index.html    # 占位页
├── .gitignore    # 忽略规则，.env 不会被上传
└── README.md     # 本文件
```

## 关于 .env

`.env` 是以后存数据库配置的**本地文件**，第 23 天才用到。
它现在还不存在，但 `.gitignore` 里已经写好忽略规则，确保它以后也不会被误传。
